# Fabric Harvester Releases

| Minecraft | Mod | Artifact |
|---|---|---|
| 26.3 | 1.8.1 | `build/libs/fabric_harvester-1.8.1.jar` |

Run `./gradlew clean release` (or `gradlew.bat clean release` on Windows)
to build the mapping-free Minecraft 26.3 artifact and write its SHA-256 checksum
to `build/checksums/fabric_harvester-1.8.1.jar.sha256`.
